import { InsightProductDemo } from "@/components/insight/InsightProductDemo";

export const metadata = {
  title: "Finance Buddy — Trợ lý đầu tư",
  description: "Demo UI: Insight + Context + Action theo struct sản phẩm",
};

export default function InsightPage() {
  return <InsightProductDemo />;
}
