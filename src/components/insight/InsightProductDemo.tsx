"use client";

import { FireAntInsightShell } from "./fireant/FireAntInsightShell";

/** Trang demo sản phẩm — bố cục kiểu FireAnt, nội dung insight đa danh mục. */
export function InsightProductDemo() {
  return <FireAntInsightShell />;
}
